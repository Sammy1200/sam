# 当前主线稳定基线

本文用于说明当前主线已经明确具备的能力边界，避免把阶段成果写大，也避免把“已合并”误写成“绝对不可动”。

---

# 基线事实

当前主线应被视为已经具备以下稳定能力：
- 正式入口固定为：
  - `.\scripts\register_scheduled_task.ps1`
  - `.\古墓迷途.cmd`
  - `.\scripts\run_via_task.ps1`
- 启动器当前已支持三种入口：
  - 登录界面启动
  - 启动页上架模式
  - 临时抢购模式
- 抢购、上架、换号/换区、悬浮窗显示仍是主线能力范围
- 不影响抢购主循环仍是最高优先级
- SQLite 仍是唯一真实数据源
- 昵称仍是唯一键
- 56 分钟轻量落库规则仍然有效
- 2 小时 50 分统计口径仍限定在真正抢购循环活跃态
- 远端镜像仍不是数据真源
- `已准备` 已是正式状态值，不是显示映射；当前写入 SQLite 真源与网页状态下拉

---

# 已合回主线并应被视为当前能力的线程/阶段

以下内容已进入当前主线，应按“当前能力”描述，而不是写成仍在独立分支中：
- 线程 2：统计口径统一
- 线程 3：账号读取 + 冷却判断 + 等待逻辑
- 线程 4 第一批：悬浮窗正常模式纯显示增强
- 线程 5：轻量落库 + 最终写回
- 线程 6：完整主体已完成
- 线程 7：当前阶段已完成，包含网页查看、单账号最小修改、`main.py` 自动静默拉起网页服务
- 线程 8：已完成，本机换号配置外置
- 线程 10B：已完成，临时抢购模式
- 启动页上架模式：已进入当前主线，按执行位 `1 -> 8` 顺序筛选低余额且非 `账号限制` 账号，独立完成上架批次与账号切换；全部扫描完成后自动切回正常模式继续抢购链路
- 线程 12：第一阶段已完成，包含双机网页汇总与 2 号电脑只读镜像
- 线程 13：已完成，本机昵称模板资源、区域、阈值配置外置
- 线程 14：历史阶段曾落到“远端最小写回设计与镜像刷新链路”；但当前正式口径已经收口为“公网只用于查看，并且只开放刷新按钮”，不开放其他修改

当前与账号状态相关的主线口径还包括：
- 当当前状态为 `账号限制` 或 `抢购时长已到`，且冷却剩余时间 <= 0 时，会自动恢复为 `已准备`
- 当网页手动把状态改成 `已准备` 时，可运行时间恢复为 2 小时 50 分，且冷却剩余时间 = 0

---

# 只算阶段完成，不要误写成完整系统完成

以下表述需要克制，不要扩大：
- “线程 2 已完成”不等于所有统计指标都可以任意改口径
- “线程 3 已完成”不等于所有读取链路都可以脱离既有冷却与等待规则重写
- “线程 4 第一批已完成”不等于悬浮窗整体改造已全部完成
- “线程 5 已完成”不等于持久化链路后续不可调整
- “线程 7 当前阶段已完成”不等于网页系统后续所有阶段都已完成
- “线程 12 第一阶段已完成”不等于双机协同、远端数据治理、同步体系已完整完成
- “线程 14 第一阶段已完成”不等于远端已经成为通用写入口，更不等于共享 SQLite 或双向同步已完成
- “本机配置外置已完成”不等于所有环境差异治理都已经结束
- “临时抢购模式已完成”不等于所有抢购模式都已经封板

---

# 已合并，但不应写成绝对稳定不可动

以下内容虽然已经合并进主线，但不应被文档写成“绝对稳定不可动”，正确表述应是“当前规则有效，后续如有明确需求可谨慎演进”：
- 统计口径统一后的统计展示与汇总方式
- 账号读取、冷却判断、等待逻辑的外围实现
- 悬浮窗正常模式纯显示增强
- 轻量落库与最终写回实现
- 网页查看能力与单账号最小修改能力
- 本机换号配置外置
- 本机昵称模板资源 / 区域 / 阈值外置
- 本机 live 数据与本机配置主入口已统一收口到 `C:\py666`，缺失时才回退项目根目录
- 双机网页汇总第一阶段
- 2 号电脑只读镜像
- 双机网页汇总与公网快照的当前正式边界：公网只用于查看，并且只开放刷新按钮，不开放其他修改
- 临时抢购模式

这些能力的共同边界是：
- 可以在明确需求下继续优化
- 但任何后续调整都不能破坏当前主线硬规则
- 尤其不能影响抢购主循环、真源边界、统计口径和落库节奏
