# 项目说明

这是一个 Windows 游戏自动化脚本项目，当前主线目标保持为：
- 自动抢购
- 自动上架
- 自动换号/换区
- 悬浮窗状态显示

项目入口：
- `main.py`

统一要求：
- 悬浮窗文案、日志文案、代码注释全部使用简体中文
- 接到任务后，先读本文件，再按任务范围读取必要文件
- 非跨模块任务，不要默认扫描整个项目
- 每次修改、优化、收尾前，必须检查是否需要同步更新 `AGENTS.md` 与 `docs` 知识库；如果本次判断无需更新，必须在收尾说明中明确写出原因

---

# 正式启动 / 验收入口

首次机器初始化只允许使用：
- `.\scripts\register_scheduled_task.ps1`

日常启动 / 实机验收 / Codex 验收只允许使用：
- `.\古墓迷途.cmd`
- `.\scripts\run_via_task.ps1`

禁止使用：
- `python main.py`
- `py main.py`
- `.\.venv\Scripts\python.exe main.py`

---

# 最高优先级红线

1. 任何修改、优化、更新，都以不影响抢购主循环为第一准则。
2. 非用户明确要求，禁止改动抢购主循环的核心结构、核心点击流程、核心检测区域、核心匹配阈值、关键时序参数。
3. 下列事实属于当前主线硬规则，文档与实现必须保持一致：
   - SQLite 是唯一真实数据源
   - 昵称是唯一键
   - 56 分钟轻量落库不能变
   - 2 小时 45 分只统计真正抢购循环活跃态
   - 远端镜像不是真源
   - `已准备` 是正式状态值，不是显示映射；写入 SQLite 真源与网页状态下拉
4. 现有模板资源默认只读，仅在用户明确要求时，才允许新增或替换模板资源。
5. 不要破坏计划任务启动器链路。
6. 禁止引入新的第三方依赖，除非用户明确要求。
7. 敏感凭据与真实本机值，未经用户明确要求不要修改。
8. 禁止默认全项目扫描。
9. 禁止把无关小任务扩展成全局重构。

---

# 模块边界

- `main.py`：唯一入口，负责提权、启动、资源加载、模式选择、换号后衔接上架与抢购
- `config.py`：所有常量、坐标、区域、阈值、等待时间、路径
- `state.py`：所有全局可变状态变量
- `utils.py`：底层键鼠、sleep、剪贴板、GC 工具、悬浮窗 mini/restore
- `vision.py`：截图裁剪、模板匹配、OCR、余额识别、容量识别
- `overlay.py`：OSD 悬浮窗、日志显示、计分板、暂停控制
- `jishujiaoben/`：F12 暂停态可手动呼出的计数悬浮窗脚本与图片资源
- `listing.py`：自动上架子系统
- `purchase.py`：抢购主循环 + 余额/限制检测 + 推送
- `switch.py`：换号/换区流程
- `account_db.py`：账号库读写与账号记录管理
- `round_persistence.py`：轮次统计、轻量落库、最终写回相关持久化逻辑
- `account_view_repo.py`：网页视图层所需账号数据聚合与读取接口
- `web_view_server.py`：本地网页查看服务入口与路由
- `web_view_templates.py`：网页显示模板与页面渲染片段
- `local_switch_account_config.py`：本机换号配置读取与适配入口
- `logo/`：模板资源目录，默认只读；仅在用户明确要求时，才允许新增或替换资源
- `scripts/register_scheduled_task.ps1`：首次初始化注册计划任务
- `scripts/task_launcher.ps1`：计划任务启动器链路脚本
- `scripts/run_via_task.ps1`：日常启动 / 验收统一底层入口
- `古墓迷途.cmd`：日常启动 / 验收批处理入口

补充规则：
- 全局可变状态只在 `state.py` 中定义，其他模块通过 `import state` 读写 `state.xxx`
- `config.py` 只放常量和配置，不写业务逻辑，不导入 `os` 以外模块
- `state.py` 只放状态变量，不写函数

---

# 默认读取范围

接到任务后：
1. 先读 `AGENTS.md`
2. 如果任务是“第一次理解项目 / 知识梳理 / 文档建设”，先读 `docs/knowledge_base/README.md`，再按推荐顺序读 `00 -> 01 -> 02 -> 03 -> 04`
3. 再按任务范围读取必要文件
4. 只有任务确实涉及多个模块时，才扩大读取范围

默认范围如下：
- 抢购问题：`purchase.py` + `config.py`，必要时 `main.py` / `vision.py` / `state.py`
- 换号问题：`switch.py` + `config.py`，必要时 `main.py`
- 上架问题：`listing.py` + `config.py`，必要时 `vision.py` / `main.py`
- 悬浮窗问题：`overlay.py` + `state.py`
- 启动/运行问题：`main.py` + 报错信息 + 运行命令
- 小范围参数修改：优先看 `config.py` 和对应模块，不要扩读无关文件
- 本机配置问题：先看 [`docs/local_config.md`](docs/local_config.md)，再读取对应示例文件或调用链
- 网页/双机汇总问题：先看 [`docs/web_and_sync.md`](docs/web_and_sync.md)，再读取相关最小范围文件

---

# 本机配置与 Git 规则

1. 本机换号配置、本机昵称模板目录、本机昵称识别区域/阈值已外置，先看 [`docs/local_config.md`](docs/local_config.md)。
2. 当前统一 live 根目录为 `C:\py666`；本机 SQLite 真源、本机换号配置、本机网页同步配置只认该目录，不再回退项目根目录。
3. 当前统一昵称模板目录主入口为 `C:\py666\nichen`；仅当该目录缺失时，才允许继续回退到其他已存在目录。
4. 示例文件用于说明结构，真实本机配置不能被示例文件覆盖。
5. 真实本机专属配置、真实本机模板资源、运行态产物，以及 `desktop.ini` 这类本机系统文件，默认不提交 Git。
6. 当前项目默认在正式项目根目录直接修改，不创建 worktree、不创建分支；只有用户明确要求时，才允许创建 worktree 或分支。
7. 不要因为看到本机差异就回滚为历史版本，先区分误改与用户明确要求。
8. 不要覆盖、删除、重置你未确认归属的现有改动。

---

# 高敏感区

## 一、抢购主循环

- 非用户明确要求，不改 `purchase.py` 核心循环结构与关键时序参数
- 禁止擅自修改：
  - `CONFIRM_DELAY`
  - `PRE_EXIT_CLICK_DELAY`
  - `EXIT_DELAY`
  - `MISMATCH_EXIT_DELAY`
- 涉及计数器逻辑时，必须检查换号后是否正确清零
- 运行时间结束后的继续抢购余额阈值当前固定为 `5 亿`
- 若已进入“时间已到但余额超过 `5 亿` 继续抢购”，后续又命中 `账号限制`，则 `last_limit_time` 必须按账号限制当前时刻重新计时 `24 小时 01 分`
- 当前抢购价格规则以本机 `local_switch_account_config.json` 为准；价格前缀支持 1 位或 2 位配置，执行顺序固定为“两位前缀优先、一位前缀次之、都未命中再看完整价格”，完整价格区间仍按本机配置中的上下限且边界不含等于；`purchase_price_skip_item_click_prefixes` 命中后直接抢，但第一识别区会跳过“点击商品”直接购买确认；修改后需重启脚本生效

## 二、悬浮窗

- 启动器选择任意模式后，必须先确认大悬浮窗已完全隐藏，才允许进入后续截图识别和点击链路
- 正常模式必须保留计分板区域和日志区
- Mini 模式只允许隐藏组件，不允许 destroy 组件
- 恢复正常模式时，必须确保组件恢复显示且内容可刷新
- F12 暂停参数窗支持按分钟设置自动恢复倒计时；点击应用后参数窗收起为标题条并显示 `恢复 HH:MM:SS`，手动 F12 恢复会取消倒计时
- F9 计数悬浮窗只允许在 F12 暂停期间呼出；再次按 F9 必须关闭并停用计数窗；若计数窗已打开，按 F12 恢复主脚本前必须先自动关闭计数窗
- 显示增强优先放显示层，业务逻辑不要塞进 `overlay.py`
- 本次新增/修改到的悬浮窗日志，字符宽度必须控制在 12 个中文字符以内
- 长期规则：以后悬浮窗日志字符宽度不得超过 12 个中文字符
- 悬浮窗计分板滚动/兜底文本显示宽度不得超过 15 个中文字符，即宽度 30；超宽时必须简化，静态标题不属于本规则的简化对象

## 三、换号/换区流程

- 换号成功后，必须先执行完整上架流程，再进入抢购循环
- 换号成功后，必须清零至少以下计数器：
  - `fail_count`
  - `limit_count`
  - `unknown_page_count`
- 当前固定规则：
  - `ACCOUNT_LIMIT_THRESHOLD = 5`
  - `unknown_page_count` 阈值 = 5
- 步骤 6-8 属于关键流程，非明确需求不要改顺序
- 正常抢购链路中，昵称识别校对成功后若目标账号冷却未归零，必须停留选区页等待，mini 悬浮窗显示 `冷却 HH:MM:SS`，归零后再点击启动游戏；等待期间不累计 2 小时 45 分运行时间
- 换号后，昵称识别校对成功且冷却等待结束后，账号状态自动改成 `运行中`
- 线程 6 的切区后昵称模板校验若首次失败，不立即暂停；必须从“打开大区列表 -> 选择目标大区 -> 昵称模板校验”开始额外重试 `3` 次，任意一次成功即继续后续进场链路，`3` 次都失败后才走现有暂停/推送出口
- 古墓大厅判定统一使用 `RGN_GUMU` + `logo/huanhao/gumudating.png`
- 当需要重新返回古墓大厅时，统一执行：点击 `(1888, 20)` 与按 `Esc` 交叉各 3 次、每次间隔 `50ms`，再点 `(825, 700)`，最后按古墓大厅判定规则校验；若首次失败，自动再重试 1 次
- 领取金币流程已改为找色链；第 1 步未找到金币入口时直接进入返回交易行，不执行关闭面板/返回古墓大厅动作；金币链中途找色失败时先返回古墓大厅，只有返回古墓大厅本身失败才暂停并推送
- 本次链路内未设固定延迟的新增/改写点击，统一先等待 `100ms`；返回古墓大厅链中的交叉点击/按键继续按各自 `50ms` 固定间隔执行
- 抢购主循环若触发 `余额不足`，必须先执行一次“返回古墓大厅 -> 尝试领取金币 -> 返回交易行 -> 重新识别余额”；若金币入口未命中，视为没有金币可领取，不推送，直接继续原换号流程；若补金币后余额恢复到阈值以上，则取消本次换号
- 若补金币后仍然 `余额不足`，则该状态也按限制类状态处理：写入 `last_limit_time`、网页显示冷却倒计时、可运行时间归 `0`、冷却结束后自动恢复为 `已准备`
- 任何 `need_switch_server=True` 的结束原因，在真正执行退出游戏换号前，都必须先尝试一次“最新余额刷新 -> SQLite 最小写库 -> 事件快照同步”；若金币入口未命中则不推送直接换号，其余刷新失败不再微信推送，只记本地日志后继续换号
- 抢购主循环遇到未知页面时，先尝试返回古墓大厅 `3` 次；成功后返回交易行继续流程，失败后才回落到现有 `unknown_page_count` 未知页面治理，不直接强制换号

## 四、账号状态与网页状态修改

- `已准备` 是新的正式状态值，不是显示映射
- 自动恢复规则固定为：
  - 当前状态为 `余额不足`、`账号限制` 或 `抢购时长已到`
  - 且冷却剩余时间 <= 0
  - 自动恢复为 `已准备`
- 网页手动把状态改成 `已准备` 时，必须同时满足：
  - 可运行时间恢复为 2 小时 45 分
  - 冷却剩余时间 = 0

## 五、临时抢购模式

- 临时模式入口从启动页开始；启动器不再输入目标执行位
- 临时模式启动时要求当前启动页已可见 `qidong.png`，直接点击启动并复用后续进场到交易行链路
- 临时模式 `baseline_item_count` 固定从 `0` 开始
- 临时模式仅在命中以下结束条件时进入定向切换链：
  - `余额不足`
  - `抢购时长已到`
  - `账号限制`
- 临时模式下号后的目标执行位选择，必须与启动页上架模式切回正常模式的选择逻辑一致
- 定向切换链固定为：
  - 自动选择目标执行位
  - 直接切到目标账号组
  - 再切目标执行位对应大区
  - 再做昵称校验
  - 昵称校验成功后继续进入后续链路
- 临时模式继续不接回 canonical 单账号主表
- 临时模式运行态昵称固定显示为 `临时号`；数据写入 `thread6_runtime.sqlite3` 的辅助临时快照，不写入 canonical `account_stats`
- 查看页本机栏目和公网页对应电脑 Snapshot 栏目可显示 `临时号`，该行只读，不参与 1-8 执行位缺失/重复健康检查；账号状态按正常账号状态口径展示为 `运行中`、`余额不足`、`抢购时长已到`、`账号限制` 等；临时模式结束后保留最后一次快照，下一次临时模式启动时覆盖
- 临时模式事件会纳入机器级汇总：
  - `总抢购成功`
  - `总上架成功`
  - `总抢购失败`
  - `总道具变化` 继续按现有展示规则派生，不单独存字段

## 六、上架前余额门槛

- 每次真正进入上架流程前，直接使用最后一次有效余额判断
- 当最后一次有效余额 `> 8亿` 时，本次直接跳过上架
- 取消“换号后首次上架强制识别余额并按 `> 8亿` 停掉本轮后续上架”的特殊规则
- 每个账号每轮上架成功上限固定为 `100`；达到上限后，本轮停止后续自动上架
- 全场景识别需要支持在 `(241, 42, 111, 32)` 区域匹配 `logo/shangjia/shangjiaye1.png` 判定上架页面；抢购主循环异常治理命中上架页面时按 `Esc` 回交易行
- 普通上架提交验证失败后，必须先处理首次 `logo/shangjia/tishi.png` 提示弹窗；在上架页面命中并点击消除成功后，按本次上架成功计数；未命中时再在 `(369, 226, 151, 48)` 区域用 `logo/shangjia/shangjia1.png` 最多补点 `(1388, 800)` 3 次；补点成功按本次上架成功计数，仍失败后做局部场景识别，若仍在上架页面则直接继续上架
- 这条规则只影响上架执行与否，不额外新增余额 OCR，不改抢购主循环逻辑
- 启动页上架模式中余额 `> 5亿` 只作为停止本号继续上架的原因；按“余额超5亿”收尾时必须保留账号原状态，不得改成 `已准备`
- 启动页上架模式进场后余额未识别、或批次返回其他异常/失败原因时，必须按 `未知异常` 写入账号状态
- 上架链路中若容量已满，先看 `(480, 140)` 到 `(585, 175)` 的倒计时区域，只判断小时两位是否为 `46/47`；高置信命中 `46/47` 时不下架；高置信判定小时不等于 `46/47` 或识别结果为 `None` 时都执行下架；执行下架前先额外等待 `500ms`，下架确认后再固定等待 `500ms`，若可上架数量未 `+1` 则按 `1` 秒间隔重试 3 次，仍未 `+1` 时跳过校验继续后续操作；只有下架容量校验 `confirmed` 时，才允许把道具库存 `+1` 并同步 SQLite；若一次下架校验已确认成功，则下一次执行下架前还需额外等待 `1.5` 秒；若上下架反复循环则暂停并提示
- 启动页上架模式使用专用写库收尾；不得让外层抢购最终收尾把账号状态覆盖为 `未知异常`

---

# 验收规则

1. 先区分本次修改属于文档、轻改动、还是必须实跑的流程改动。
2. 以下场景可以不运行：
   - 仅修改文档文件
   - 仅修改注释
   - 仅修改 print / 日志文字
   - 仅修改 `config.py` 中已有常量值且未新增/删除变量
   - 用户明确要求“不用运行”
3. 以下场景先做轻验证：
   - 单模块小改动
   - 局部函数修复
   - 小范围显示层修改
   - 配置读取链路微调
4. 以下场景必须实际运行验证：
   - 修改运行流程
   - 修改主入口
   - 修改换号流程
   - 修改上架流程
   - 修改悬浮窗恢复逻辑
   - 修改抢购主循环相关逻辑
   - 修改跨模块联动逻辑
5. 禁止把静态检查表述成运行验证。
6. 如果运行失败，只能如实说明关键报错摘要和失败位置。

---

# 当前明确未纳入范围

1. 不要把 `AGENTS.md` 写成线程日记或阶段流水账。
2. [`docs/thread_history.md`](docs/thread_history.md) 只作为历史档案，不作为硬规则来源。
3. 线程阶段完成不等于“整个系统已经全部完成”，当前能力判断以 [`docs/current_baseline.md`](docs/current_baseline.md) 为准。
4. 当前不做双向同步、不共享 SQLite；公网快照只用于查看与刷新按钮，不开放其他修改，详见 [`docs/web_and_sync.md`](docs/web_and_sync.md)。

---

# 附加文档导航

- [`docs/knowledge_base/README.md`](docs/knowledge_base/README.md)：项目知识库入口，第一次接手项目时优先从这里开始
- [`docs/knowledge_base/00_start_here.md`](docs/knowledge_base/00_start_here.md)：5 分钟总览，先看项目用途、正式启动方式、真源边界
- [`docs/knowledge_base/01_system_overview.md`](docs/knowledge_base/01_system_overview.md)：系统主流程、数据流、整体边界
- [`docs/knowledge_base/02_module_map.md`](docs/knowledge_base/02_module_map.md)：模块职责、不负责什么、改动注意点
- [`docs/knowledge_base/03_runtime_and_data.md`](docs/knowledge_base/03_runtime_and_data.md)：canonical、运行态、派生字段、状态语义
- [`docs/knowledge_base/04_operations_and_acceptance.md`](docs/knowledge_base/04_operations_and_acceptance.md)：正式运行、验收、live 路径与排错入口
- [`docs/current_baseline.md`](docs/current_baseline.md)：当前主线稳定基线、已合回主线的线程/阶段、哪些能力已具备但不应夸大
- [`docs/web_and_sync.md`](docs/web_and_sync.md)：网页查看、最小修改、静默拉起、本地与双机汇总边界、远端只读镜像规则
- [`docs/local_config.md`](docs/local_config.md)：本机换号配置、昵称模板目录、昵称识别区域/阈值外置，以及示例文件与真实文件边界
- [`docs/thread_history.md`](docs/thread_history.md)：线程阶段总结、实验结论、合并状态、验收状态，仅作历史档案
