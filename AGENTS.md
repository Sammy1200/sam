# 项目说明
这是一个 Windows 游戏自动化脚本项目，目标是完成：
- 自动抢购
- 自动上架
- 自动换号/换区
- 悬浮窗状态显示

项目入口：
- `main.py`

首次机器初始化命令：
- `.\scripts\register_scheduled_task.ps1`

日常启动 / 实机验收 / Codex 验收入口：
- `.\古墓迷途.cmd`
- 底层入口：`.\scripts\run_via_task.ps1`

---

# 最高优先级

**任何修改、优化、更新，都以不影响抢购主循环为第一准则。**

要求：
1. 修改任何文件前，先判断是否会影响抢购循环的稳定性、时序、识别准确性和执行效率。
2. 非用户明确要求，禁止改动抢购主循环的关键时序参数、核心点击逻辑、核心检测区域、核心匹配阈值。
3. 如果某个方案可能让抢购循环变慢，优先换方案，不要硬改。
4. 修改完成后，必须检查是否意外影响了抢购主循环相关代码。

---

# 模块边界

- `main.py`：唯一入口，负责提权、启动、资源加载、模式选择、换号后衔接上架与抢购
- `config.py`：所有常量、坐标、区域、阈值、等待时间、路径
- `state.py`：所有全局可变状态变量
- `utils.py`：底层键鼠、sleep、剪贴板、GC工具、悬浮窗 mini/restore
- `vision.py`：截图裁剪、模板匹配、OCR、余额识别、容量识别
- `overlay.py`：OSD悬浮窗、日志显示、计分板、暂停控制
- `listing.py`：自动上架子系统
- `purchase.py`：抢购主循环 + 余额/限制检测 + 推送
- `switch.py`：换号/换区流程
- `backup_original.py.py`：只读参考基准，不直接修改
- `logo/`：模板资源目录，只读，不修改

---

# 默认读取范围

接到任务后：
1. **先读本 AGENTS.md**
2. 再按任务范围读取必要文件
3. 非跨模块任务，**不要默认扫描整个项目**
4. 只有任务确实涉及多个模块时，才扩大读取范围

默认范围如下：

- 抢购问题：`purchase.py` + `config.py`，必要时 `main.py` / `vision.py` / `state.py`
- 换号问题：`switch.py` + `config.py`，必要时 `main.py`
- 上架问题：`listing.py` + `config.py`，必要时 `vision.py` / `main.py`
- 悬浮窗问题：`overlay.py` + `state.py` + `backup_original.py.py`
- 启动/运行问题：`main.py` + 报错信息 + 运行命令
- 小范围参数修改：优先看 `config.py` 和对应模块，不要扩读无关文件

---

# 核心规则

1. 全局可变状态只在 `state.py` 中定义，其他模块通过 `import state` 后读写 `state.xxx`
2. `config.py` 只放常量和配置，不写业务逻辑，不导入 `os` 以外的模块
3. `state.py` 只放状态变量，不写函数
4. 坐标、区域、阈值、等待时间、重试次数等可配置项，优先放到 `config.py`
5. 修改时优先最小改动，能改一处不改多处
6. 禁止删除现有功能，只允许局部修复、增强或配置化
7. 代码注释使用中文
8. 函数命名使用 snake_case
9. 关键操作必须有日志或 print，包括但不限于：
   - 退出游戏
   - 启动游戏
   - 换号流程
   - 计数器清零
   - 上架触发
10. 所有 dxcam 取帧优先走统一封装，避免直接写分散取帧逻辑
11. 所有等待逻辑应兼容暂停状态，不要写无法中断的长阻塞等待
12. 新增功能如果涉及多个模块，必须说明每个模块改了什么

---

# 三个敏感区特别规则

## 一、抢购主循环（最高敏感区）
1. 非用户明确要求，禁止改动以下内容：
   - 抢购主循环结构
   - 核心点击流程
   - 核心检测区域
   - 核心匹配阈值
   - 关键时序参数
2. 禁止擅自修改这些时序参数的值：
   - `CONFIRM_DELAY`
   - `PRE_EXIT_CLICK_DELAY`
   - `EXIT_DELAY`
   - `MISMATCH_EXIT_DELAY`
3. 如果某次任务只是外围功能、显示增强、统计增强，不要顺手改 `purchase.py`
4. 计数器相关逻辑变动后，必须检查换号后是否正确清零

## 二、悬浮窗（高敏感区）
1. 修改悬浮窗前，必须先参考 `backup_original.py.py`
2. 正常模式必须保留：
   - 计分板区域
   - 日志区
3. Mini 模式只允许隐藏组件，不允许 destroy 组件
4. 恢复正常模式时，必须确保组件能恢复显示，内容能刷新
5. 悬浮窗增强优先做“显示增强”，不要把业务逻辑塞进显示层
6. 后续新增“手工初始值、账号信息、结果摘要”时，优先通过 `state.py` 传值，悬浮窗只负责显示

## 三、换号/换区流程（高敏感区）
1. 换号成功后，必须先执行完整上架流程，再进入抢购循环
2. 换号成功后，必须清零跨账号残留计数器，至少包括：
   - `fail_count`
   - `limit_count`
   - `unknown_page_count`
3. `ACCOUNT_LIMIT_THRESHOLD = 5`
4. `unknown_page_count` 的处理阈值按 **5** 作为当前规则
5. 步骤 6-8 属于关键流程，非明确需求不要改动顺序
6. 换号流程优先最小修改，坐标和区域优先配置化到 `config.py`

---

# 当前项目固定业务值

以下值作为当前项目规则，文档和代码必须保持一致：

- `ACCOUNT_LIMIT_THRESHOLD = 5`
- `unknown_page_count` 阈值 = 5
- 自动上架间隔 = **56 分钟**
- 换号成功后必须重新上架
- 首次机器初始化命令：`.\scripts\register_scheduled_task.ps1`
- 日常启动 / 实机验收 / Codex 验收入口：`.\古墓迷途.cmd`
- 底层入口：`.\scripts\run_via_task.ps1`

---

# 红线（绝对不可违反）

1. 禁止修改 `logo/` 目录下任何图片文件
2. 禁止修改 `backup_original.py.py`
3. 禁止移除管理员提权逻辑
4. 禁止引入新的第三方依赖，除非用户明确要求
5. 禁止修改 pushplus token
6. 禁止改变模块依赖方向，避免循环导入
7. 禁止在 `config.py` 写函数或业务逻辑
8. 禁止在 `state.py` 写函数
9. 禁止把“静态检查”描述成“运行验证”
10. 禁止默认全项目扫描
11. 禁止把无关小任务扩展成全局重构
12. 禁止在没有明确需要的情况下改动抢购主循环核心逻辑

---

# 验证规则

## 一、运行命令规则
首次机器初始化只允许使用：
- `.\scripts\register_scheduled_task.ps1`

日常启动 / 实机验收 / Codex 验收只允许使用：
- `.\古墓迷途.cmd`
- `.\scripts\run_via_task.ps1`

禁止使用：
- `python main.py`
- `py main.py`
- `.\.venv\Scripts\python.exe main.py`

## 二、分层验证规则

### 1. 无需运行验证的情况
以下场景可以不实跑：
- 仅修改文档文件
- 仅修改注释
- 仅修改 print / 日志文字
- 仅修改 `config.py` 中已有常量值，且未新增/删除变量
- 用户明确要求“不用运行”

这类情况应明确说明：
- “本次为文档/注释/常量微调，无需运行验证”

### 2. 优先做轻验证的情况
以下场景先做轻验证即可：
- 单模块小改动
- 局部函数修复
- 小范围显示层修改
- 配置读取链路微调

轻验证包括：
- import 是否报错
- 调用链是否明显断裂
- 函数签名是否保持兼容
- 受影响文件之间是否存在明显引用错误

### 3. 必须实际运行验证的情况
以下场景必须实际运行验证：
- 修改了运行流程
- 修改了主入口
- 修改了换号流程
- 修改了上架流程
- 修改了悬浮窗恢复逻辑
- 修改了抢购主循环相关逻辑
- 修改了跨模块联动逻辑

如果运行失败：
1. 先贴关键报错摘要
2. 说明失败位置
3. 不要伪造“已运行成功”

---

# 输出要求

接到任务后，输出应尽量遵循以下顺序：

1. 先说明本次准备修改哪些文件
2. 再说明修改思路
3. 如是 bug 修复，先指出问题根因
4. 最后给最小补丁或修改结果
5. 修改完成后说明：
   - 改了什么
   - 为什么改
   - 怎么验证
6. 如果需要扩大读取范围，先说明原因
7. 如果任务描述不清晰，先向用户提问，不要自行猜测需求

---

# 回滚规则

1. 回滚前，必须区分：
   - 误改
   - 用户主动要求的功能改进
2. 如果无法判断某处改动属于哪类，必须先问用户
3. 以下方向默认视为用户明确要求的功能规则，不要擅自回滚：
   - 账号限制触发换号
   - `ACCOUNT_LIMIT_THRESHOLD = 5`
   - `unknown_page_count` 阈值 = 5
   - 换号成功后重新上架
   - 换号后清零跨账号残留计数器
4. 不要因为“看起来像旧逻辑”就自动回滚为历史版本

---

# 工作方式

1. 先读 AGENTS.md
2. 再按任务范围读取必要文件
3. 优先最小修改
4. 优先保持现有功能稳定
5. 先问清，再动手
