# 项目说明

这是一个 Windows 游戏自动化脚本项目，当前主线目标保持为：
- 自动抢购
- 自动上架
- 自动换号/换区
- 悬浮窗状态显示

项目入口：
- `main.py`

统一要求：
- 悬浮窗文案、日志文案全部使用简体中文
- 接到任务后，先读本文件，再按任务范围读取必要文件
- 非跨模块任务，不要默认扫描整个项目

---

# 正式启动 / 验收入口

首次机器初始化只允许使用：
- `.\scripts\register_scheduled_task.ps1`

日常启动 / 实机验收 / Codex 验收只允许使用：
- `.\古墓迷途.cmd`
- `.\scripts\run_via_task.ps1`

禁止使用：
- `python main.py`
- `py main.py`
- `.\.venv\Scripts\python.exe main.py`

---

# 最高优先级红线

1. 任何修改、优化、更新，都以不影响抢购主循环为第一准则。
2. 非用户明确要求，禁止改动抢购主循环的核心结构、核心点击流程、核心检测区域、核心匹配阈值、关键时序参数。
3. 下列事实属于当前主线硬规则，文档与实现必须保持一致：
   - SQLite 是唯一真实数据源
   - 昵称是唯一键
   - 56 分钟轻量落库不能变
   - 2 小时 50 分只统计真正抢购循环活跃态
   - 远端镜像不是真源
   - `已准备` 是正式状态值，不是显示映射；写入 SQLite 真源与网页状态下拉
4. 现有模板资源默认只读，仅在用户明确要求时，才允许新增或替换模板资源。
5. 禁止修改 `backup_original.py.py`。
6. 不要破坏计划任务启动器链路。
7. 禁止引入新的第三方依赖，除非用户明确要求。
8. 敏感凭据与真实本机值，未经用户明确要求不要修改。
9. 禁止默认全项目扫描。
10. 禁止把无关小任务扩展成全局重构。

---

# 模块边界

- `main.py`：唯一入口，负责提权、启动、资源加载、模式选择、换号后衔接上架与抢购
- `config.py`：所有常量、坐标、区域、阈值、等待时间、路径
- `state.py`：所有全局可变状态变量
- `utils.py`：底层键鼠、sleep、剪贴板、GC 工具、悬浮窗 mini/restore
- `vision.py`：截图裁剪、模板匹配、OCR、余额识别、容量识别
- `overlay.py`：OSD 悬浮窗、日志显示、计分板、暂停控制
- `listing.py`：自动上架子系统
- `purchase.py`：抢购主循环 + 余额/限制检测 + 推送
- `switch.py`：换号/换区流程
- `account_db.py`：账号库读写与账号记录管理
- `round_persistence.py`：轮次统计、轻量落库、最终写回相关持久化逻辑
- `account_view_repo.py`：网页视图层所需账号数据聚合与读取接口
- `web_view_server.py`：本地网页查看服务入口与路由
- `web_view_templates.py`：网页显示模板与页面渲染片段
- `local_switch_account_config.py`：本机换号配置读取与适配入口
- `backup_original.py.py`：只读参考基准，不直接修改
- `logo/`：模板资源目录，默认只读；仅在用户明确要求时，才允许新增或替换资源
- `scripts/register_scheduled_task.ps1`：首次初始化注册计划任务
- `scripts/task_launcher.ps1`：计划任务启动器链路脚本
- `scripts/run_via_task.ps1`：日常启动 / 验收统一底层入口
- `古墓迷途.cmd`：日常启动 / 验收批处理入口

补充规则：
- 全局可变状态只在 `state.py` 中定义，其他模块通过 `import state` 读写 `state.xxx`
- `config.py` 只放常量和配置，不写业务逻辑，不导入 `os` 以外模块
- `state.py` 只放状态变量，不写函数

---

# 默认读取范围

接到任务后：
1. 先读 `AGENTS.md`
2. 再按任务范围读取必要文件
3. 只有任务确实涉及多个模块时，才扩大读取范围

默认范围如下：
- 抢购问题：`purchase.py` + `config.py`，必要时 `main.py` / `vision.py` / `state.py`
- 换号问题：`switch.py` + `config.py`，必要时 `main.py`
- 上架问题：`listing.py` + `config.py`，必要时 `vision.py` / `main.py`
- 悬浮窗问题：`overlay.py` + `state.py` + `backup_original.py.py`
- 启动/运行问题：`main.py` + 报错信息 + 运行命令
- 小范围参数修改：优先看 `config.py` 和对应模块，不要扩读无关文件
- 本机配置问题：先看 [`docs/local_config.md`](docs/local_config.md)，再读取对应示例文件或调用链
- 网页/双机汇总问题：先看 [`docs/web_and_sync.md`](docs/web_and_sync.md)，再读取相关最小范围文件

---

# 本机配置与 Git 规则

1. 本机换号配置、本机昵称模板目录、本机昵称识别区域/阈值已外置，先看 [`docs/local_config.md`](docs/local_config.md)。
2. 当前统一 live 根目录为 `C:\py666`；本机 SQLite 真源、本机换号配置、本机网页同步配置优先从该目录读取，缺失时再回退项目根目录。
3. 当前统一昵称模板目录主入口为 `C:\py666\nichen`；仅当该目录缺失时，才允许继续回退到其他已存在目录。
4. 示例文件用于说明结构，真实本机配置不能被示例文件覆盖。
5. 真实本机专属配置、真实本机模板资源、运行态产物，默认不提交 Git。
6. 不要因为看到本机差异就回滚为历史版本，先区分误改与用户明确要求。
7. 不要覆盖、删除、重置你未确认归属的现有改动。

---

# 高敏感区

## 一、抢购主循环

- 非用户明确要求，不改 `purchase.py` 核心循环结构与关键时序参数
- 禁止擅自修改：
  - `CONFIRM_DELAY`
  - `PRE_EXIT_CLICK_DELAY`
  - `EXIT_DELAY`
  - `MISMATCH_EXIT_DELAY`
- 涉及计数器逻辑时，必须检查换号后是否正确清零
- 运行时间结束后的继续抢购余额阈值当前固定为 `2 亿`
- 已启用余额异常下跳保护：
  - 阈值外置在 `config.py`
  - 当本次识别余额比上一次有效余额突然减少超过阈值时，保留上一次有效余额
  - 这条规则只拦“异常大下跳”，不拦上涨

## 二、悬浮窗

- 修改前必须先参考 `backup_original.py.py`
- 正常模式必须保留计分板区域和日志区
- Mini 模式只允许隐藏组件，不允许 destroy 组件
- 恢复正常模式时，必须确保组件恢复显示且内容可刷新
- 显示增强优先放显示层，业务逻辑不要塞进 `overlay.py`

## 三、换号/换区流程

- 换号成功后，必须先执行完整上架流程，再进入抢购循环
- 换号成功后，必须清零至少以下计数器：
  - `fail_count`
  - `limit_count`
  - `unknown_page_count`
- 当前固定规则：
  - `ACCOUNT_LIMIT_THRESHOLD = 5`
  - `unknown_page_count` 阈值 = 5
- 步骤 6-8 属于关键流程，非明确需求不要改顺序
- 换号后，昵称识别校对成功时，账号状态自动改成 `运行中`

## 四、账号状态与网页状态修改

- `已准备` 是新的正式状态值，不是显示映射
- 自动恢复规则固定为：
  - 当前状态为 `账号限制` 或 `抢购时长已到`
  - 且冷却剩余时间 <= 0
  - 自动恢复为 `已准备`
- 网页手动把状态改成 `已准备` 时，必须同时满足：
  - 可运行时间恢复为 2 小时 50 分
  - 冷却剩余时间 = 0

## 五、临时抢购模式

- 临时模式入口已移除“输入当前道具数量”，改为输入“目标执行位”
- 临时模式 `baseline_item_count` 固定从 `0` 开始
- 临时模式仅在命中以下结束条件时进入定向切换链：
  - `余额不足`
  - `抢购时长已到`
  - `账号限制`
- 定向切换链固定为：
  - 直接切到目标账号组
  - 再切目标执行位对应大区
  - 再做昵称校验
  - 昵称校验成功后继续进入后续链路
- 临时模式继续不接回 canonical 单账号主表
- 临时模式事件会纳入机器级汇总：
  - `总抢购成功`
  - `总上架成功`
  - `总抢购失败`
  - `总道具变化` 继续按现有展示规则派生，不单独存字段

---

# 验收规则

1. 先区分本次修改属于文档、轻改动、还是必须实跑的流程改动。
2. 以下场景可以不运行：
   - 仅修改文档文件
   - 仅修改注释
   - 仅修改 print / 日志文字
   - 仅修改 `config.py` 中已有常量值且未新增/删除变量
   - 用户明确要求“不用运行”
3. 以下场景先做轻验证：
   - 单模块小改动
   - 局部函数修复
   - 小范围显示层修改
   - 配置读取链路微调
4. 以下场景必须实际运行验证：
   - 修改运行流程
   - 修改主入口
   - 修改换号流程
   - 修改上架流程
   - 修改悬浮窗恢复逻辑
   - 修改抢购主循环相关逻辑
   - 修改跨模块联动逻辑
5. 禁止把静态检查表述成运行验证。
6. 如果运行失败，只能如实说明关键报错摘要和失败位置。

---

# 当前明确未纳入范围

1. 不要把 `AGENTS.md` 写成线程日记或阶段流水账。
2. [`docs/thread_history.md`](docs/thread_history.md) 只作为历史档案，不作为硬规则来源。
3. 线程阶段完成不等于“整个系统已经全部完成”，当前能力判断以 [`docs/current_baseline.md`](docs/current_baseline.md) 为准。
4. 当前不做双向同步、不共享 SQLite、不允许远端写回真源，详见 [`docs/web_and_sync.md`](docs/web_and_sync.md)。

---

# 附加文档导航

- [`docs/current_baseline.md`](docs/current_baseline.md)：当前主线稳定基线、已合回主线的线程/阶段、哪些能力已具备但不应夸大
- [`docs/web_and_sync.md`](docs/web_and_sync.md)：网页查看、最小修改、静默拉起、本地与双机汇总边界、远端只读镜像规则
- [`docs/local_config.md`](docs/local_config.md)：本机换号配置、昵称模板目录、昵称识别区域/阈值外置，以及示例文件与真实文件边界
- [`docs/thread_history.md`](docs/thread_history.md)：线程阶段总结、实验结论、合并状态、验收状态，仅作历史档案
